
export interface StlSettings {
  baseHeight: number;
  modelHeight: number;
  invert: boolean;
  maxSize: number; // Max width/height in pixels for processing
}

export interface Vector3 {
    x: number;
    y: number;
    z: number;
}
